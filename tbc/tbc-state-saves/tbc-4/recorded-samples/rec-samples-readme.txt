Samples recorded using the two audio inlets on THE BEAT COP are saved into this directory.

This README file exists to make sure that Github preserves this folder on commit. If I uploaded this folder without a file in it, Github would ignore it, and it would not be in the repo.